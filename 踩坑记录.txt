1. Buf初始化
2. strtok_s会修改原字符串
3. cat命令申请空间时没有初始化为0，导致输出时结尾有乱码
4. 删除用户时d_uid写成了uid
5. 删除无用户组时，gmsg.size() = 2 ，访问gmsg[2]越界
6. 和windows传输文件时，变量名fname2写错

优化记录
1. Toolfun新增路径寻找、参数检查、输入密码三个函数
2. 报错优化，避免直接退出系统